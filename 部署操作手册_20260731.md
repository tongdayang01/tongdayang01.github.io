# 全量内容迁移部署操作手册

> **目标：** `https://tongdayang01.github.io` 地址栏 0 变化，评审专家打开即见完整内容  
> **架构：** GitHub Pages（透明 iframe 壳）+ 腾讯云 COS + CDN（实际内容）  
> **日期：** 2026-07-31

---

## 一、前置准备清单

| # | 事项 | 状态 |
|---|------|------|
| 1 | 腾讯云账号（已实名认证） | ☐ |
| 2 | 腾讯云 COS 服务已开通 | ☐ |
| 3 | 腾讯云 CDN 服务已开通（可选但强烈建议） | ☐ |
| 4 | 本地部署包已就绪：`E:/AI文档/website_tencent/` | ✅ |
| 5 | GitHub 仓库推送权限（账号/Token） | ☐ |
| 6 | GitHub Pages 启用权限 | ☐ |

---

## 二、腾讯云 COS 部署（核心步骤）

### 2.1 创建 COS 存储桶

1. 登录 [腾讯云 COS 控制台](https://console.cloud.tencent.com/cos)
2. 点击「创建存储桶」
3. 配置如下：
   - **名称：** `yangtongda-award`（或自定义，需全局唯一）
   - **所属地域：** `广州（ap-guangzhou）`
   - **访问权限：** `公有读私有写`
   - **其他：** 默认即可
4. 点击「创建」

### 2.2 开启静态网站托管

1. 进入存储桶 → 基础配置 → 静态网站
2. 点击「编辑」
3. 开启「静态网站」开关
4. 配置：
   - **索引文档：** `index.html`
   - **错误文档：** `index.html`
   - **重定向规则：** 留空
5. 点击「保存」

### 2.3 配置 CORS（跨域访问）

1. 进入存储桶 → 安全管理 → 跨域访问 CORS 设置
2. 点击「添加规则」
3. 配置：
   ```
   来源 Origin: *
   允许 Methods: GET, HEAD
   Allow-Headers: *
   Expose-Headers: Content-Length, Content-Type
   Max-Age: 86400
   ```
4. 点击「保存」

### 2.4 配置响应头（最关键！iframe 能否加载取决于此）

1. 进入存储桶 → 安全管理 → 响应头设置
2. 点击「添加规则」
3. **对所有 .html 文件添加：**
   ```
   Content-Security-Policy: frame-ancestors https://tongdayang01.github.io
   Cache-Control: no-cache, must-revalidate
   ```
4. **对所有图片/视频/文档文件添加：**
   ```
   Cache-Control: public, max-age=2592000
   ```

### 2.5 上传文件

#### 方式一：控制台拖拽上传（推荐）
1. 进入存储桶 → 文件列表
2. 将 `E:/AI文档/website_tencent/` 中**所有文件和文件夹**拖入上传区域
3. 点击「上传」
4. 等待完成

#### 方式二：CLI 命令行上传
```bash
# 安装 coscmd
pip install coscmd

# 配置
coscmd config -a <SecretId> -s <SecretKey> -b <bucket-name> -r ap-guangzhou

# 上传所有文件
coscmd upload -r "E:/AI文档/website_tencent/" / --skipmd5
```

### 2.6 获取默认访问域名

上传完成后，文件列表上方显示「默认CDN加速域名」或「默认域名」，格式类似：
```
https://yangtongda-award-1234567890.cos.ap-guangzhou.myqcloud.com
```
**记录此域名**，后续步骤需要使用。

---

## 三、CDN 加速配置（强烈推荐）

### 3.1 添加 CDN 加速域名

1. 进入 [CDN 控制台](https://console.cloud.tencent.com/cdn)
2. 点击「域名管理」→「添加域名」
3. 配置：
   - **加速域名：** 您的自定义域名（如 `award.yourdomain.com`），或使用 COS 默认域名
   - **服务类型：** 静态加速
   - **源站类型：** COS 源站
   - **源站地址：** 选择刚创建的存储桶
4. 完成域名归属验证（DNS TXT 记录或文件验证）
5. 等待部署（通常 5-10 分钟）

### 3.2 配置 HTTPS 证书

1. CDN 控制台 → 域名管理 → 点击域名 → HTTPS 配置
2. 申请免费 SSL 证书（腾讯云提供）或上传已有证书
3. 开启「强制 HTTPS 跳转」

### 3.3 配置 CDN 缓存规则

CDN 控制台 → 域名管理 → 缓存配置 → 添加缓存规则：

| 类型 | 路径 | 缓存时间 |
|------|------|----------|
| 文件类型 | `.html` | **0 天**（不缓存） |
| 文件类型 | `.jpg;.png;.gif;.svg;.webp;.ico` | 30 天 |
| 文件类型 | `.mp4;.webm` | 30 天 |
| 文件类型 | `.js;.css` | 30 天 |
| 文件类型 | `.pdf;.doc;.docx;.xlsx` | 7 天 |
| 文件夹 | `/charts/` | 0 天 |
| 全部 | `*` | 1 天 |

### 3.4 配置 HTTP 响应头（CDN 层面）

CDN 控制台 → 域名管理 → 高级配置 → HTTP 响应头：

```
Content-Security-Policy: frame-ancestors https://tongdayang01.github.io
```

**此配置与 COS 响应头双重确保，为最关键步骤！**

---

## 四、GitHub Pages 壳部署

### 4.1 推送壳文件

**方式一：运行推送脚本**
```bat
E:\AI文档\website_github_shell\push_to_github.bat
```

**方式二：手动推送**
```bash
cd "E:\AI文档\website"
git add index.html 404.html .nojekyll
git commit -m "透明代理壳：iframe嵌入腾讯云CDN完整内容 | 2026-07-31"
git push origin main:master
```

**方式三：网页端直接上传**
1. 打开 https://github.com/tongdayang01/tongdayang01.github.io
2. 点击「Add file」→「Upload files」
3. 将 `E:/AI文档/website_github_shell/` 中 3 个文件拖入上传区域
4. Commit message 填写："透明代理壳 v1.0"
5. 点击「Commit changes」

### 4.2 修改壳文件中的腾讯云域名

在推送前，务必修改 `index.html` 和 `404.html` 中第 17 行的占位符：

**当前（占位符）：**
```javascript
var COS_BASE = '__TENCENT_CLOUD_DOMAIN__';
```

**修改为实际域名（示例）：**
```javascript
var COS_BASE = 'https://yangtongda-award-1234567890.cos.ap-guangzhou.myqcloud.com';
```

或使用 CDN 自定义域名：
```javascript
var COS_BASE = 'https://award.your-custom-domain.com';
```

### 4.3 启用 GitHub Pages

1. 打开 https://github.com/tongdayang01/tongdayang01.github.io/settings/pages
2. **Source：** 「Deploy from a branch」
3. **Branch：** 选择 `master`（或 `main`），目录选 `/ (root)`
4. 点击「Save」
5. 等待 1-2 分钟，页面顶部显示 "Your site is live at https://tongdayang01.github.io"

---

## 五、验证测试清单

### 5.1 腾讯云 COS 验证

```bash
# 验证主页可访问
curl -sI "https://<cos-domain>/index.html"

# 验证 CSP 响应头存在（最关键！）
curl -sI "https://<cos-domain>/index.html" | findstr "frame-ancestors"

# 验证子页面
curl -sI "https://<cos-domain>/about.html"
curl -sI "https://<cos-domain>/charts/index.html"

# 验证资源文件
curl -sI "https://<cos-domain>/videos/main_presentation.mp4"
```

### 5.2 GitHub Pages 验证

| # | 测试场景 | URL | 预期结果 |
|---|----------|-----|----------|
| 1 | 首页完整加载 | `https://tongdayang01.github.io` | 地址栏不变，全屏显示网站首页 |
| 2 | 子页面 | `https://tongdayang01.github.io/about` | 地址栏不变，显示关于我们 |
| 3 | 图表页 | `https://tongdayang01.github.io/charts/` | 地址栏不变，显示图表导航 |
| 4 | 视频页 | `https://tongdayang01.github.io/videos.html` | 地址栏不变，视频可播放 |
| 5 | 图片直链 | `https://tongdayang01.github.io/assets/campus-aerial.jpg` | 自动跳转，图片正常显示 |
| 6 | 右键查看源码 | 同上 | 看到透明壳的简短HTML代码 |
| 7 | F12 开发者工具 | 同上 | iframe src 指向腾讯云域名 |
| 8 | 移动端 | 微信浏览器打开 | 正常显示，无异常提示 |

### 5.3 关键验证：iframe 未拦截

1. 打开 `https://tongdayang01.github.io`
2. F12 → Console
3. 确保**没有**以下错误：
   ```
   Refused to frame 'https://...' because an ancestor violates
   the following Content Security Policy directive: "frame-ancestors ..."
   ```
4. 如果看到此错误 → CSP 响应头未配置正确，返回步骤 2.4 和 3.4

---

## 六、文件结构速查

### GitHub Pages 壳仓库（仅 3 个文件）
```
tongdayang01.github.io/
  ├── index.html      # 全屏iframe透明壳（~7KB）
  ├── 404.html        # 子路径处理器（与index.html相同）
  └── .nojekyll       # 禁用Jekyll处理
```

### 腾讯云 COS 部署包结构
```
/
├── index.html            # 主站首页（原版，已修复CDN依赖）
├── about.html            # 关于我们（已去除Google Fonts）
├── videos.html           # 视频页面（视频引用已更新）
├── summary.html          # 总结报告
├── application.html      # 申报书
├── speech.html           # 主题演讲
├── theory.html           # 理论框架详解
├── curriculum.html       # 产教融合课程设计
├── cases.html            # 典型案例
├── stories.html          # 成长故事
├── worldskills.html      # 世赛服务
├── architecture.html     # 技术架构
├── ai-system.html        # AI系统文档
├── generator.html        # AI教学设计生成器
├── zhican.html           # 值餐系统
├── yushe.html            # 悦舍系统
├── platform.html         # 平台生态
├── cultural-courses.html # 文化基础课
├── promotion.html        # 推广应用
├── evidence.html         # 佐证材料
├── team.html             # 成果完成人
├── awards.html           # 获奖证明
├── media.html            # 相关报道
├── assets/               # 51张证书/照片图片（~12MB）
│   ├── *.jpg             # 证书、照片、截图等
│   └── videos/           # 5个宣传视频（~77MB）
│       ├── promo_auto.mp4
│       ├── promo_cook.mp4
│       ├── promo_cook2.mp4
│       ├── promo_drone.mp4
│       └── promo_yimei.mp4
├── videos/               # 主视频和场馆视频（~69MB）
│   ├── main_presentation.mp4    # 主申报视频（10分钟有声版）
│   ├── stage_drama.mp4          # 情景舞台剧（480p）
│   └── principal_speech.mp4     # 校长讲话（480p）
├── charts/               # 核心图表集（6+1个HTML文件）
│   ├── index.html                # 图表导航页
│   ├── 图1_十三年四阶段鱼骨图.html
│   ├── 图2_三阶递进+五维+四真三化.html
│   ├── 图3_三问题+四方案全景图.html
│   ├── 图4_三大创新点理论架构.html
│   ├── 图5_推广四级跃迁+成效数据.html
│   └── 图6_教师五阶+产教全栈.html
├── diagrams/             # PNG高清图（6个文件，~2.3MB）
│   ├── 图1~6 对应PNG文件
├── lib/                  # CDN备选库
│   ├── js/               # Three.js/GSAP/ECharts 本地备选
│   └── fonts/            # 字体说明
└── 部署操作手册_20260731.md  # 本文件
```

---

## 七、常见问题排查

### Q1: 打开 github.io 显示空白页
**原因：** COS 的 CSP 响应头未正确配置  
**解决：** 返回步骤 2.4 检查 COS 响应头是否包含 `frame-ancestors https://tongdayang01.github.io`  
**验证：** `curl -sI https://<cos-domain>/index.html | findstr "frame-ancestors"`

### Q2: 页面显示 "___ refused to connect"
**原因：** COS 存储桶未设置为「公有读」  
**解决：** COS 控制台 → 权限管理 → 存储桶访问权限 → 设置为「公有读私有写」

### Q3: 子路径页面（如 /about）显示404
**原因：** GitHub Pages 未正确配置 SPA 回退  
**解决：** 确保 `404.html` 文件已推送且内容与 `index.html` 完全相同

### Q4: 图片/视频无法加载
**原因：** 文件路径不匹配或未上传  
**解决：** COS 控制台确认对应文件是否存在；路径是否匹配大小写

### Q5: Three.js/GSAP/ECharts 动画不显示
**原因：** jsdelivr CDN 在国内被限速  
**解决：** 下载 JS 文件到 `lib/js/` 目录并上传；或等待加载（jsdelivr 有国内节点）

---

## 八、紧急备用方案

如果 `https://tongdayang01.github.io` 完全无法访问（如被运营商封锁），请：

1. **立即在申报材料中附上备用地址：**
   > 腾讯云CDN直达：`https://<您的COS或CDN域名>/index.html`
   > 
   > 如原网址不可达，评审专家可直接访问以上备用地址查看全量内容。

2. **联系评审组织方：**
   > 说明因技术原因，原 `github.io` 域名在国内可能存在访问波动，已配置自动跳转和备用入口，确保评审不受影响。

3. **监控告警：**
   > 在腾讯云 CDN 控制台开启访问日志和 5xx 告警，确保内容服务不中断。

---

## 九、成本估算

| 项目 | 月费（估算） |
|------|-------------|
| COS 存储（~170MB 文件） | ¥0.02 |
| CDN 流量（假设 10 人完整浏览） | ¥2-5 |
| CDN HTTPS 请求 | ¥0.1 |
| SSL 证书 | ¥0（腾讯云免费证书） |
| **合计** | **<¥10/月** |

---

> **部署完成后，请务必逐项验证步骤五中的 8 个测试场景。**  
> **如有任何问题，参考第七节排查，或检查 CSP 响应头是否正确配置。**
